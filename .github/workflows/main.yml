name: Build & Push ecs-exporter chart
on: push

jobs:
  push_to_registries:
    name: Push Docker image to multiple registries
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Push Helm Chart to ChartMuseum
        uses: bsord/helm-push@4.1.0
        with:
          username: 'klin'
          password: '19120252'
          registry-url: 'http://94.26.229.183:8080'
          force: true
          chart-folder: './'


